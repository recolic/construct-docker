# construct docker

## Build

```bash
cd build
sudo ./makepkg.sh
sudo docker build -t recolic/construct .
cd ..
```

And you're all done!

## Usage

```bash
sudo docker run -tid --restart=always --TODO
```

